import styled from 'styled-components';

const BarWrapper = styled.div`
  display: flex;
  align-items: center;
  width: 100%;
  height: 5px;
  background: linear-gradient(to right, #ee4036, #ffdd15);
`;

export default BarWrapper;
