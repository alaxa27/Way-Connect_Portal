/*
 * Fidelity Messages
 *
 * This contains all the text for the Fidelity component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.Fidelity';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Fidelity component!',
  },
});
