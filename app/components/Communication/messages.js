/*
 * Communication Messages
 *
 * This contains all the text for the Communication component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.Communication';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Communication component!',
  },
});
