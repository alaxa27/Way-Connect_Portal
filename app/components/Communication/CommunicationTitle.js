import styled from 'styled-components';

const CommmunicationTitle = styled.div`
  color: ${props => props.theme.colors.text};
  font-size: 0.75rem;
  font-weight: lighter;
`;

export default CommmunicationTitle;
