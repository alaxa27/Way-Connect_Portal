/*
 * Ttt Messages
 *
 * This contains all the text for the Journey container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Journey';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Journey container!',
  },
});
